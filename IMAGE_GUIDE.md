# 画像追加ガイド

## 📁 フォルダ構成

```
assets/
└── other/
    ├── ui/          # UI/UXデザイン作品
    ├── motion/      # モーショングラフィックス
    └── system/      # システム開発
```

---

## 🖼️ 画像の追加方法

### 1. 画像を用意

- **ファイル名**: `{カテゴリ}_{番号2桁}.{拡張子}`
  - 例: `UI_01.png`, `MOTION_03.jpg`, `SYSTEM_05.webp`
- **対応拡張子**: `png`, `jpg`, `jpeg`, `webp`
- **推奨サイズ**: 横幅1280px以上（16:9の場合）

### 2. フォルダに配置

```
assets/other/ui/UI_03.png
```

### 3. 確認

- Live Serverで即座に反映
- プレースホルダが消えて画像が表示される

### 4. デプロイ

```powershell
npm run deploy
```

---

## 📋 カテゴリ別スロット数

| カテゴリ | スロット数 | ファイル名例 |
|---------|----------|------------|
| UI/UX Design | 12 | `UI_01.png` 〜 `UI_12.png` |
| Motion Graphics | 8 | `MOTION_01.jpg` 〜 `MOTION_08.jpg` |
| System Development | 8 | `SYSTEM_01.webp` 〜 `SYSTEM_08.webp` |

---

## 🔧 スロット数の変更方法

`data/page-main.js`を編集：

```javascript
{
    id: "ui",
    title: "UI/UX Design",
    slots: 20,  // 12 → 20 に変更
    basePath: "assets/other/ui/UI_",
    aspectRatio: "16/9"
}
```

保存すると、`UI_01` 〜 `UI_20` まで表示されます。

---

## 🎨 プレースホルダ表示

画像が配置されていないスロットは、以下のように表示されます：

- 薄い枠線と背景
- 中央にラベル（例: `UI_03`, `MOTION_05`）
- 主張しすぎないデザイン

---

## 💡 運用のコツ

### サイトを見ながら判断

1. ブラウザでサイトを開く
2. 「UI_03に入れたい」と判断
3. `assets/other/ui/UI_03.png`を配置
4. 自動的に反映される

### 複数の拡張子に対応

以下の順番で自動的に探します：
1. `.png`
2. `.jpg`
3. `.jpeg`
4. `.webp`

最初に見つかったものを使用します。

---

## 🚨 トラブルシューティング

### 画像が表示されない

**確認項目**:
1. ファイル名が正しいか（例: `UI_03.png`）
2. フォルダが正しいか（`assets/other/ui/`）
3. 拡張子が対応しているか（png, jpg, jpeg, webp）
4. ブラウザをリロードしたか（Ctrl+R）

### プレースホルダが消えない

**原因**: 画像ファイルが見つからない

**解決策**:
1. ファイル名を確認
2. 拡張子を確認
3. ブラウザのコンソールでエラーを確認

---

## 📝 まとめ

- **画像を置くだけ**: `assets/other/ui/UI_03.png`を配置
- **即座に反映**: Live Serverで自動更新
- **簡単デプロイ**: `npm run deploy`で公開
- **拡張性**: スロット数やカテゴリを簡単に追加可能
