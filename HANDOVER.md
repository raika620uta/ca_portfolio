# プロジェクト引継ぎ資料（HANDOVER / ハンズアップ）

このドキュメントは、本ポートフォリオ制作プロジェクトの現状・思想・課題。および今後の具体的なアクションを、第三者や次回のAIが完全に理解し、即座に実務に着手できるようにまとめたものです。

---

## ① プロジェクト概要
- **目的**: サイバーエージェント（CA）の内定獲得を目的とした、AI×縦型動画広告の専門性をアピールするポートフォリオ。
- **ターゲット**: CAの採用担当、AI広告チーム。
- **背景**: 既存の「感覚的な動画編集者」とは一線を画し、「AIを構造的に理解し、再現可能なフローで高クオリティなアウトプットを作る」能力を証明する。

## ② コアコンセプト（最重要）
- **AIガチャからの脱却**: AIを単なる運任せの生成ツールとして使わず、制御可能な「素材生成器」として定義。
- **フローとしてのAI**: 個別のAI生成物ではなく、AIを組み込んだ「制作パイプライン（仕組み）」そのものを提示する。
- **AI×AEの住み分け**:
  - **AI**: 構成・シナリオ・生素材（ビデオ・静止画）の生成。
  - **After Effects (AE)**: 最終的なレイアウト・文字デザイン・リズムの担保。
- **再現性・量産前提**: 1本が偶然できるのではなく、100本を同じ質で（あるいは自動で）作れる構造であることを示す。

## ③ 現在のサイト構成
- **セクション構造 (Top to Bottom)**:
  1. **Profile Intro**: クッションとしての柔らかな導入（円形プロフィール画像 + 日本語/英語名 + 一言コピー）。
  2. **Hero Section**: 強いキャッチコピー（「AIガチャからの脱却。」）。現在は画面左端からの強い視線誘導を採用中。
  3. **WORK-FLOW**: 制作思想の文章化と図解。
  4. **Cases (Main Projects)**: Case01から順に、詳細な制作プロセス（フック比較・AE構造等）を提示。
  5. **Other Works**: グラフィック、UI、3D、映像作品など。
- **UI基本思想**: 白と黒を基調としたミニマルデザイン。装飾（影・グラデ・不要なアニメ）を排除し、情報の強さで魅せる。

## ④ 実装状況（具体詳細）
### 完了していること
- **Data-Driven 構造**: `data/page-main.js` にすべてコンテンツ（セクション、事例データ）を集約。
- **Renderer 方式**: `engine/renderer.js` がデータを読み込み、HTMLを動的に生成。セクションの入れ替えや追加がデータ更新のみで可能。
- **縦動画（9:16）対応**: 広告素材をそのままの画角で表示し、`IntersectionObserver` により画面内に入った瞬間に自動再生される制御を実装。
- **画像ガイド (`IMAGE_GUIDE.md`)**: 画像サイズやアスペクト比を、今後どう差し替えるかのルールを策定済み。

### 不具合・技術的負債
- **Heroレイアウトの脆弱性**: 過去に `.container` 構造を改変しようとして崩壊した経緯あり。現在は `git reset` 等で復興を繰り返している。
- **brタグの処理不備**: HTMLタグをエスケープする `nl2br(esc())` 処理の都合で、タグが文字として出てしまうことがあった（最新の対応では `span` 分割で暫定解決）。
- **シークバーの操作性**: `video` タグのデフォルトに依存しており、モバイル等での視認性に課題。

## ⑤ 制作ルール
- **構造優先**: レイアウトをいじる前に、データの構造（HTML/DOM）がセマンティックであるかを優先する。
- **1変更＝1意図**: まとめて「レイアウトとフォントを整える」といった作業は禁止。Gitコミット単位で分離すること。
- **AI出力＝70点**: サイト上の見た目だけで 100点を目指さない。AEで仕上げた後の「完成形」への証拠を重視する。
- **レイアウト大改修禁止**: 現在の `.container` およびセクション余白（`clamp` 等）は、手動でのピクセル指定を極力避ける。

## ⑥ 禁止事項
- **`.container` の再設計**: paddingや幅の設定をグローバルに変えない。
- **「良かれと思って」の改変**: 行間やフォント、色の微細な変更はユーザーの指示がない限り行わない。
- **不必要に複雑なJS**: jQuery等は不要。Vanilla JSでのDOM操作を維持する。

## ⑦ 現在の優先タスク
1. **Case01の情報の詰め込み**: AEでのレイアウト、フック（冒頭2秒）の比較画像など、CAが最も重視する「構成の意図」を厚くする。
2. **フック比較の言語化**: なぜA案よりB案が優れているかを、定量的（あるいは構造的）に述べる。
3. **制作フロー図の精密化**: 「AIが何をし、人間が何をしたか」の境界線を明確にする。

## ⑧ 次にやるべき具体アクション
1. **`data/page-main.js` 内の Case01 配列を埋める**: 
   - `images` 配列に実際のプレースホルダー名を入れ、それぞれの `caption` を「構成の意図」に書き換える。
2. **フロー図画像の差し替え**: 手書きのラフではなく、仕組み（System Diagram）に見える図表を用意して差し替える。
3. **AEの Before/After 比較**: AI生成素材のみの状態と、AEでタイポグラフィを載せた後の状態をサイドバイサイドで並べる。

## ⑨ 技術詳細
- **言語**: HTML5, CSS3, Vanilla JavaScript (ES6)
- **管理**: GitHub Pages / Git
- **ディレクトリ構造**:
  - `data/`: `page-main.js` (コンテンツ本体)
  - `engine/`: `renderer.js` (DOM生成), `style.css` (全デザイン)
  - `assets/`: 画像・動画素材（9:16比率厳守）

## ⑩ 現在の完成度
- **進捗率**: 約 45%
- **不足分**: 各事例の「思考の深さ」を示すドキュメント（文章・図解）、および本物の素材への差し替え。

## ⑪ このポートフォリオの強み
- **「AIを使える」だけでなく「AIを制御できる」点**: 制作過程を見せることで、採用側が「この人は再現性を持ってチームに参加できる」と直感できるよう設計されている。

---
以上。この情報を元に、次回のAIは「データの充実」と「既存レイアウトの保持」に注力してください。
